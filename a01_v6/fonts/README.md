# Fonts

Michroma, Inter Tight and IBM Plex Mono, from Google Fonts. All three are under the SIL Open Font License 1.1,
which allows them to be redistributed with this project. `mograph.py` loads them from this folder by default
(override with `A01V6_FONTS`).
